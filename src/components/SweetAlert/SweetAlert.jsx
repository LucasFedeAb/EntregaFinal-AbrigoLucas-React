const SweetAlert = ({ idOrder, formData, createOrder }) => {
  const {
    firstName,
    lastName,
    email,
    confirmEmail,
    city,
    address,
    province,
    zip,
    phone,
  } = formData;
  return <></>;
};

export default SweetAlert;
